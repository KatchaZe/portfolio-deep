# Code Review — รอบที่ 2 (ตรวจ แล้วแก้)

ตรวจหลังปิด backlog ครบ (`REV-1`…`REV-28`) · เทสต์ 45 ชุดผ่านหมด → หลังแก้เป็น 46 ชุด
เป้าหมายตามที่สั่ง: หา bug ที่ **ทำงานถูกตามคำสั่งแต่ทำให้ผลเพี้ยน** — ค่าคงที่, ค่าที่ดึงไม่ได้, ค่าที่เป็น 0

**ไม่สะอาด** — พบ 3 จุดที่ควรแก้ (1 จุดสำคัญ อยู่ในโค้ดที่ผมเพิ่งเขียนเอง) และ 5 จุดที่ควรรู้ไว้

> **สถานะ: แก้ครบแล้ว** (tag `P2-1` … `P2-5` ในโค้ด) · เทสต์ **46 ชุด ผ่านหมด**
> รายละเอียดการแก้ที่หัวข้อ 6–7 ท้ายไฟล์ · จุด 🟡 ต่ำ (`L1`–`L5`) ยัง**ไม่แก้โดยตั้งใจ** อยู่ที่หัวข้อ 4

| # | จุด | ระดับ | ผลกระทบ | สถานะ |
|---|---|---|---|---|
| P2-1 | SBC dilution ใช้ proxy ที่ผิดทิศได้ | 🔴 สูง | MSFT/ABBV โดนโทษที่ไม่ควรโดน (proxy ผิดทิศ) | ✅ หัวข้อ 6 |
| P2-2 | นับ SBC ซ้ำบนเส้น FVP | 🔴 สูง | FVP ต่ำเกิน ~12% | ✅ หัวข้อ 6 |
| P2-3 | `dWC` ใช้ขาที่ไม่ครบเหมือนครบ — เครื่องหมายพลิกได้ | 🟠 กลาง | FCFF สูงเกิน เมื่อ filer แท็กแค่ AP | ✅ หัวข้อ 6 |
| P2-4 | ช่วง Monte Carlo ±6pp เป็นค่าคงที่ที่ตัดสินว่าได้เป็น anchor ไหม | 🟠 กลาง | เกณฑ์วัดสมมติฐาน ไม่ใช่วัดบริษัท | ✅ หัวข้อ 6 |
| **P2-5** | **routing เพี้ยนจาก REV-23 — ORCL ไปเข้าโมเดล young company** | 🔴 **สูง** | **ORCL FV $22.68 vs ราคา $213** | ✅ **หัวข้อ 7** |
| L1–L5 | ดูหัวข้อ 4 | 🟡 ต่ำ | — | รับทราบ ไม่แก้ |

**P2-5 เจอจากการรันบนเครื่องจริง ไม่ใช่จากเทสต์** — เทสต์ 46 ชุดผ่านหมดทั้งก่อนและหลัง

**ส่วนที่ตรวจแล้วสะอาด:** store round-trip · facts schema เก่า · JSON serialisation ·
AR check เมื่อประวัติสั้น · `pick()` บนตัวเลขติดลบ · peak scan resolution ·
zero/None propagation (ไม่ crash สักเคส) · REV-26 sector cap

---

## P2-1 · SBC dilution: proxy ผิดทิศ + นับซ้ำ 🔴

**นี่คือโค้ดที่ผมเขียนเองใน REV-1 และมันมีปัญหาสองชั้นที่แยกกันคนละเรื่อง**

### ชั้นที่ 1 — proxy วัดคนละอย่างกับที่ควรวัด

โค้ดใช้ `annual dilution ≈ SBC$ / market cap` ซึ่งถูกทางคณิตศาสตร์สำหรับ
*หุ้นที่แจกออกไป* แต่ตัวที่ไปหาร EPS จริง ๆ คือ **share count สุทธิหลังหักซื้อคืน**

ข้อมูลจริงจาก fixture (share-count series มีครบ 10–18 ปีทุก filer สหรัฐ):

| ticker | SBC | mcap | proxy ที่ใช้อยู่ | **share count จริง** | ทิศ |
|---|---|---|---|---|---|
| AVGO | $7.57B | $1,500B | 0.50%/ปี | **+1.57%/ปี** | ตรงกัน (แต่ต่ำไป 3x) |
| MSFT | $11.97B | $3,100B | 0.39%/ปี | **−0.05%/ปี** | 🔴 **ตรงข้าม** |
| ORCL | $4.67B | $490B | 0.95%/ปี | **+1.52%/ปี** | ตรงกัน (ต่ำไป 1.6x) |
| ABBV | $0.95B | $340B | 0.28%/ปี | **+0.00%/ปี** | 🔴 **ตรงข้าม** |

MSFT แจกหุ้นปีละ 12 พันล้านดอลลาร์ **แต่ share count หดลง** เพราะซื้อคืนมากกว่าแจก
โมเดลกลับไปหักมูลค่ามันเพราะ "จ่าย SBC เยอะ" ทั้งที่ผู้ถือหุ้นเดิมไม่ได้ถูก dilute เลย

และ proxy นี้ยังแปรผกผันกับ **ราคาหุ้น** — หุ้นที่ราคาลดครึ่งจะถูกคิด dilution เป็นสองเท่า
บนเงิน grant ก้อนเดิม ซึ่งลงโทษหุ้นที่เพิ่งถูกเทขายพอดี ซึ่งเป็นกลุ่มที่ value screen มีไว้หา

```
grant เท่าเดิม $1.05B:
  mcap $40B (วันนี้)        -> dilution 2.62%/ปี
  mcap $20B (ลดครึ่ง)       -> dilution 5.25%/ปี   ← โทษหนักขึ้นเพราะราคาตก
  mcap $80B (ขึ้นเท่าตัว)    -> dilution 1.31%/ปี
```

**ข้อมูลที่ถูกต้องมีอยู่แล้วและฟรี:** `WeightedAverageNumberOfDilutedSharesOutstanding`
มี annual series 10–18 จุดในทุก fixture สหรัฐ (NVO ซึ่งเป็น IFRS ไม่มี — ต้องคง proxy
ไว้เป็น fallback)

### ชั้นที่ 2 — นับซ้ำบนเส้น FVP

กติกาใน system prompt เขียนไว้ตรง ๆ ว่า *"SBC is Real Expense (Dilution factored,
**no double counting**)"* แต่สองเส้นทางใช้ EPS คนละชนิด:

| เส้นทาง | EPS ที่ใช้ | SBC อยู่ในนั้นหรือยัง | หัก dilution เพิ่ม = ? |
|---|---|---|---|
| `fv_peg` | `forward_eps` = NTM **adjusted** consensus | ❌ non-GAAP ตัด SBC ออก | ✅ ถูกต้อง |
| `fv_fvp` | `eps0` จาก `f.net_income` | ✅ GAAP หัก SBC ไปแล้ว | 🔴 **นับซ้ำ** |

FVP หัก dilution 5 ปี = **−12% ของมูลค่า** ทับกับ SBC ที่ถูกหักไปแล้วใน net income
(`facts.py` เขียนเองว่า `forward_eps: NTM adjusted consensus`)

→ **แก้แล้ว** ตามทิศทางนี้: ใช้ share-count จริงเป็นหลัก, proxy เป็น fallback สำหรับ IFRS,
และหัก dilution เฉพาะเส้นที่ EPS ยังไม่รวม SBC · รายละเอียดที่หัวข้อ 6

---

## P2-2 · `working_capital_change` ใช้ขาที่ไม่ครบเหมือนครบ 🟠

REV-18 ตั้งใจให้ `ΔWC = ΔAR + Δสต็อก − ΔAP` และออกแบบให้ข้ามขาที่ filer ไม่ได้แท็ก
แต่ผลลัพธ์ถูกใช้เป็นตัวเลขเต็มโดยไม่มีการลดน้ำหนัก:

```
ครบสามขา          +1.00B   (dWC from AR+inventory+AP)
เฉพาะ AP          -1.00B   (dWC from AP)              ← เครื่องหมายพลิก
เฉพาะ AR          +0.80B   (dWC from AR)
```

filer ที่แท็กแค่ `AccountsPayableCurrent` จะได้ ΔWC **ติดลบ** แปลว่า "working capital
คืนเงินสดมา" ทั้งที่ความจริงคือขา AR กับสต็อกที่หักล้างกันไม่ได้ถูกยื่น
ผลคือ **reinvestment ต่ำเกิน → FCFF สูงเกิน** ซึ่งเป็นทิศที่เข้าข้างบริษัท

label บอกว่าใช้ขาไหนบ้าง แต่ตัวเลขถูกบริโภคเหมือนสมบูรณ์ · ทางที่ปลอดภัยกว่าคือ
คืน `None` เมื่อขา AR หายไป (ขาที่ใหญ่ที่สุดและมีทุก filer ที่มีรายได้)

---

## P2-3 · ช่วง Monte Carlo ±6pp คือค่าคงที่ที่ตัดสินว่า "เชื่อได้หรือไม่" 🟠

คุณเลือกกติกาว่า young DCF จะเป็น anchor ก็ต่อเมื่อ band แคบพอ (p90/p10 < 4x)
ซึ่งถูกต้อง แต่ **ความกว้างของ band ถูกกำหนดโดยค่าคงที่ที่เขียนไว้ในโค้ด ไม่ใช่โดยข้อมูล**

`monte_carlo()` สุ่ม `target_margin` แบบ triangular ที่ ±6pp แต่ความไวจริงเป็นแบบนี้:

```
target margin    going-concern/share    vs ฐาน 25%
    10%              $ 2.15               −88%
    15%              $ 7.67               −59%
    20%              $13.15               −29%
    25%              $18.60                 0%
    30%              $24.05               +29%
    35%              $29.48               +59%
```

±6pp ครอบแค่ช่วง ~19–31% → band ราว ±29% → p90/p10 ≈ 1.8x → **ผ่านเกณฑ์ 4x สบาย ๆ**
ถ้าเปลี่ยนเป็น ±12pp (ซึ่งก็ไม่ได้เกินจริงสำหรับหุ้นที่ยังขาดทุน) band จะกว้างขึ้นราวเท่าตัว
และหลายตัวจะไม่ผ่านเกณฑ์

พูดอีกอย่าง: **เกณฑ์ 4x กำลังวัดว่าเราสุ่มกว้างแค่ไหน ไม่ได้วัดว่าบริษัทนี้ประเมินยากแค่ไหน**
ตัวเลข ±6pp และ 4.0 ทั้งคู่ไม่ได้มาจากข้อมูลอะไรเลย

เกี่ยวเนื่อง: มูลค่า young DCF **100.4%** มาจาก terminal value (PV ของ 10 ปีแรกติดลบ
เพราะเผาเงิน) ซึ่งถูกต้องตามธรรมชาติของหุ้นกลุ่มนี้ แต่แปลว่าคำตอบทั้งหมดคือ
`target_margin × roic_stable` เท่านั้น — 10 ปีที่ projection ไว้แทบไม่มีผลต่อคำตอบ

---

## 4. จุดที่ควรรู้ไว้ (🟡 ต่ำ — รับทราบ ไม่แก้)

> ใช้รหัส `L1`…`L5` เพื่อไม่ให้ชนกับ `P2-1`…`P2-5` ที่เป็นจุดที่แก้ไปแล้ว

**L1 · `forward_eps_rejection` ปิดเพดานเมื่อ revenue เป็น 0 จริง**
`if revenue and shares` — filer ที่ยื่น `Revenues = 0` (biotech pre-revenue มีจริง)
ทำให้เพดาน revenue-capacity หายไปเงียบ ๆ เหลือแค่ P/E gate
รูปเดียวกับ `or 0.08` ที่ REVIEW-1 แก้ไป · ผลกระทบจริงจำกัด เพราะหุ้นแบบนี้มักตกไปเส้น
reverse DCF อยู่แล้ว แต่ confidence score ก็ไม่ถูกหักตามไปด้วย

**L2 · `implied()` คืนขอบ band (−50.0%) เป็นตัวเลขปกติ**
หุ้นที่ถูกมากจนแม้สมมติหดตัว 50%/ปี ก็ยังมีค่ามากกว่าราคา จะได้ `implied_cagr_pct = -50.0`
พร้อม verdict "Plausible" — มันคือขอบ ไม่ใช่ค่าประมาณ และไม่มีอะไรบอก
(ต่างจาก PE clamp ที่ flag ตัวเองว่าเป็น boundary)

**L3 · `p_survival` เป็นขั้นบันได**
runway 5.01 ปี → 0.85 · runway 4.99 ปี → 0.75 ต่างกัน 0.02 ปี แต่ p กระโดด 10pp
ซึ่งเลื่อนมูลค่าไป 10% ของช่องว่างระหว่าง going-concern กับ distress แบบไม่ต่อเนื่อง
(MC สุ่ม ±0.2 ทับอยู่แล้ว จึงกลบผลไปได้บางส่วน)

**L4 · `tax_rate` คืน 21% เมื่อ `tax_expense` เป็น None**
บริษัทที่ยื่น `tax_expense = 0` จริง ๆ ได้ 0.0 ถูกต้องแล้ว (band `0 <= r <= 0.6` รับ)
แต่ที่ไม่ได้ยื่นเลยได้ 21% ซึ่งกดทับ NOPAT/FCFF ของหุ้น NOL — กลุ่มเดียวกับที่
young DCF เพิ่งเริ่มตีมูลค่าให้พอดี

**L5 · `reinvest` clamp 0.8 ที่ยังเหลือใน `fundamental_growth`**
REV-23 ปลดเพดานใน gate หลักแล้ว แต่ `fundamental_growth()` ยัง clamp ที่ 0.8
ตัวนี้เป็นแค่ warning ไม่ใช่ค่ามูลค่า จึงไม่เร่งด่วน แต่เป็นเพดานทางเดียวตัวสุดท้ายที่เหลือ

---

## 5. สิ่งที่ตรวจแล้วสะอาด

| ตรวจ | ผล |
|---|---|
| zero / None ทะลุทั้ง engine (7 เคส) | ไม่ crash สักเคส · routing สมเหตุสมผลทุกเคส |
| facts schema เก่า (ก่อน REV-18/19) | evaluate ได้ปกติ · `dWC` คืน None อย่างซื่อสัตย์ |
| `to_dict()` / JSON round-trip | ครบทุก field ใหม่ · serialisable |
| AR check เมื่อประวัติสั้น | ปิดตัวเองเงียบ ๆ ถูกต้อง (บริษัทเพิ่ง IPO ทริกไม่ได้ — ตั้งใจ) |
| `pick()` บนตัวเลขติดลบ | เลือกตามลำดับ list ถูกต้อง · conflict flag ทำงาน |
| peak scan n=40 | คลาดเคลื่อน 0.37% — ละเอียดพอ |
| REV-26 sector cap | coarse hit ถูก cap ที่ 14.92% · ticker map ตรงไม่กระทบ |
| operator precedence ใน margin ramp | `(1-tax) if m_t>0 else 1.0` — ถูกตามเจตนา |
| `pick()` callers | อัปเดตครบทั้ง 3 จุดหลังเปลี่ยนเป็น 3-tuple |

---

## ข้อสังเกตปิดท้าย

สองในสามจุดที่พบรอบนี้ (P2-1, P2-2) อยู่ใน**โค้ดที่เพิ่งเขียนในรอบก่อน** และเป็นรูปแบบ
เดียวกับที่ไล่จับมาตลอด — ใช้ proxy แทนข้อมูลจริงที่มีอยู่แล้ว, และใช้ค่าที่ไม่ครบเหมือนครบ
รวมกับ REV-17 และ REV-28 ที่เจอไปแล้ว เป็น **4 จุดจากทั้งหมด 28 ที่เป็น regression
ของการแก้ในเซสชันเดียวกัน**

ข้อเสนอเชิงกระบวนการ: ทุกครั้งที่เพิ่ม proxy ให้เช็คก่อนว่า**ข้อมูลจริงมีให้ดึงไหม** —
กรณี share count มีอยู่ใน companyfacts เดียวกันที่ดึงมาแล้ว 10–18 ปี

---

## 6. การแก้ (P2-1 … P2-4) — 46 ชุดผ่านหมด

### P2-1 · ใช้ share count จริง แทน proxy

ดึง `WeightedAverageNumberOfDilutedSharesOutstanding` เป็น annual series
(อยู่ใน companyfacts ก้อนเดิมที่ดึงมาอยู่แล้ว) → `share_count_growth()` คิด CAGR 3 ปี
เพื่อไม่ให้ปีที่ซื้อคืนหนักหรือปีที่ควบรวมเป็นตัวกำหนดสมมติฐานทั้งหมด

`dilution_rate()` เลือกตามลำดับ: **ประวัติจริง → proxy (เฉพาะ filer ที่ไม่รายงาน share
count เช่น IFRS/NVO)** และรายงาน source ออกมาที่ `key_metrics.dilution_source` เสมอ

**จุดที่ต้องตัดสินใจ:** ประวัติจริงติดลบได้ (บริษัทซื้อคืนมากกว่าแจก) — ผม**floor ที่ 0**
ไม่ใช่ให้เครดิต เพราะโครงการซื้อคืนเป็นนโยบายจัดสรรทุนที่หยุดเมื่อไหร่ก็ได้ การให้เครดิต
เท่ากับ capitalize นโยบายชั่วคราวเข้าไปในมูลค่าตลอดกาล — เขียนเหตุผลไว้ใน label ด้วย

```
MSFT-like (ซื้อคืน)   -> 0.00%/yr   actual diluted share count, 3y CAGR (floored at 0: ...)
AVGO-like (dilute)   -> 1.73%/yr   actual diluted share count, 3y CAGR
IFRS ไม่มี series     -> 0.10%/yr   SBC / market cap proxy (no share-count history filed)
```

`shares_diluted_annuals` **ไม่ได้อยู่ใน `normalize._MONEY`** โดยตั้งใจ — มันคือจำนวนหุ้น
ไม่ใช่จำนวนเงิน การแปลง FX จะทำให้ NVO พัง (เขียน comment กำกับไว้แล้วกันคนมาเพิ่มทีหลัง)

### P2-2 · เลิกนับ SBC ซ้ำ

หัก dilution เฉพาะเส้นที่ EPS **ยังไม่รวม** ต้นทุน SBC:

| เส้นทาง | EPS | SBC อยู่ในนั้นแล้ว? | หัก dilution |
|---|---|---|---|
| `fv_peg` | forward_eps (NTM adjusted, non-GAAP) | ❌ | ✅ หัก |
| `fv_fvp` | eps0 จาก GAAP net income | ✅ | ❌ **ไม่หัก** |
| `fv_peg` เมื่อ validate แทนด้วย SEC | sec-derived (GAAP) | ✅ | ❌ ไม่หัก + flag |

เคสสุดท้ายสำคัญ — `validate._resolve_forward_eps()` แทน forward EPS ด้วยค่าจาก SEC
เมื่อ consensus ถูก reject ซึ่งเป็น GAAP จึงต้องไม่หักด้วย ตรวจผ่าน
`provenance["forward_eps"].startswith("sec-derived")`

ผลจริง (7 เคสทดสอบ): FVP กลับขึ้นทุกตัว — HIGHQ_LOWBETA +5.8%, SBC_HEAVY +14%,
NET_CASH +18.6% · ค่า PEG ยังถูกหักตามเดิม

### P2-3 · `dWC` ต้องมีขา AR

ขาทั้งสามมีเครื่องหมายตรงข้ามกัน การใช้ขาที่ไม่ครบจึงไม่ใช่ "ประมาณการหยาบ" แต่**ผิดทิศ**
filer ที่แท็กแค่ AP ได้ค่าติดลบ = "working capital คืนเงินสด" ทั้งที่ความจริงคือขาที่หักล้าง
มันไม่ได้ถูกยื่น AR เป็นขาที่ใหญ่ที่สุดและ filer ที่มีรายได้ทุกรายยื่น จึง**บังคับ** ส่วนสต็อก
กับเจ้าหนี้เป็นตัวเสริมเมื่อมี · ขาด AR → คืน `None` พร้อมเหตุผล

### P2-4 · ความกว้าง Monte Carlo ผูกกับว่า "วัดได้" หรือ "เดา"

```
ANCHORED (วัดจาก op margin ของบริษัทเอง)  -> สุ่ม ±4pp
ASSUMED  (ขาดทุนอยู่ ใช้ตาราง/generic 25%) -> สุ่ม ±10pp
```

เหตุผล: เดิมเป็นค่าคงที่ ±6pp เท่ากันหมด แปลว่าเกณฑ์ p90/p10 < 4x กำลังวัด**ว่าเราสุ่ม
กว้างแค่ไหน** ไม่ได้วัดว่าบริษัทนี้ประเมินยากแค่ไหน ตอนนี้ความกว้างมาจากสิ่งที่จริง —
เรามีตัวเลขของบริษัทเองหรือไม่ — และรายงานออกมาที่ `inputs.target_margin_sampled_pp`

ผลกับ YOUNGCO: band 1.76x → **2.04x** (ยังผ่านเกณฑ์ 4x จึงยังเป็น anchor ได้)
VALUE_DESTROYER: p10 −1.90 → **−2.99** และยังถูก block ด้วยเหตุผลเดิม

### กันไม่ให้ย้อนกลับมา

`tests/audit2.py` ถูกยกจาก "สคริปต์ตรวจครั้งเดียว" เป็น **guard ถาวรใน `run_tests.py`**
มันจะ exit 1 ทันทีที่พบ finding ระดับ MED/HIGH — ครอบคลุมรูปแบบที่ไล่จับมาทั้งสองรอบ
(ใช้ proxy ทั้งที่มีข้อมูลจริง / ใช้ค่าไม่ครบเหมือนครบ / ค่าคงที่ที่เป็นตัวตัดสินคำตอบ)
ส่วน finding ระดับ LOW ทั้ง 3 ถือว่ารับทราบแล้วและไม่ทำให้ build แดง

```
SUMMARY: 3 findings
  [LOW] implied() คืนขอบ band -50.0% เป็นตัวเลขปกติ
  [LOW] p_survival เป็นขั้นบันได
  [LOW] tax_rate คืน 21% เมื่อ tax_expense เป็น None
OK: 3 LOW finding(s), all documented and accepted
```

---

## 7. P2-5 · เจอตอนรันบนเครื่องจริง — routing เพี้ยนจาก REV-23

รันชุดเทสต์บน Windows แล้วเห็นบรรทัดนี้ใน `test_engine`:

```
ORCL  HOLD  ★★☆☆☆  anchor Young-Company DCF (failure-adjusted) $22.68
```

ORCL ราคา $213 ได้ fair value $22.68 จากโมเดล **young company** ทั้งที่มีกำไรสุทธิ $16.2B
ตัวเลขนี้ผิดมากพอที่จะเปลี่ยนการตัดสินใจ

**สาเหตุ — REV-23 ของผมเอง**

gate เดิมเขียนว่า `(not eps_pos) or (not fcf_pos)` และขา `fcf_pos` **ไม่เคยทำงาน**
เพราะ clamp 0.8 การันตีว่า `fcff >= 20%` ของ NOPAT เสมอ REV-23 ปลด clamp นั้น
(ซึ่งถูกต้อง — gate ที่ยิงไม่ออกคือ gate ที่ไร้ค่า) แต่**เปลี่ยน routing ไปด้วยโดยไม่ได้ตั้งใจ**

```
ORCL: capex $48.25B (สร้าง datacenter) + dWC $1.91B − D&A $6.36B
      ÷ NOPAT $16.36B  =  reinvestment 268%  →  FCFF −$27.44B  →  fcf_pos = False
```

FCFF ติดลบเป็นความจริง แต่มันคือ "ปีนี้ลงทุนหนัก" ไม่ใช่ "ไม่มีกำไรให้ตีมูลค่า"
S20 โมเดลอย่างหลัง — มัน ramp margin ขึ้นจากการขาดทุน และหา p_survival จาก cash runway
ซึ่งไม่มีความหมายเลยกับบริษัทที่ทำกำไร $16B

**แก้:** routing ดูที่ **กำไร** อย่างเดียว (`pre_profit = not eps_pos`) ส่วน FCFF ติดลบ
กลายเป็น flag เสียงดังแทนที่จะเป็นการเปลี่ยนวิธีตีมูลค่า — ประโยชน์จริงของ REV-23
(gate ยิงออกแล้ว และบอกได้ว่าเผาเงินแค่ไหน) ยังอยู่ครบ

```
ORCL:  Young-Company DCF $22.68  →  Fundamental PEG $95.68
flags: "reinvestment 268% of NOPAT (capex+M&A+dWC-D&A) - consuming more cash than it earns"
       "FCFF negative on heavy reinvestment but earnings are positive -
        valued on the normal earnings paths, not the young-company model"
```

**เทสต์กันย้อนกลับ:** `test_heavy_capex_stays_on_earnings_path` — บริษัทกำไรที่ลงทุนหนัก
ต้องไม่แตะ S20 (`young_dcf == {}`) แต่ยังต้องมี flag เรื่อง FCFF และบริษัทที่ขาดทุนจริง
ต้องยังเข้า S20 ได้เหมือนเดิม

**บทเรียนซ้ำอันเดิม:** นี่เป็น regression ตัวที่ **5** ของเซสชันนี้ที่เกิดจากการแก้ของตัวเอง
(REV-17, REV-28, P2-1, P2-2, P2-5) และทั้งหมดมีรูปแบบเดียวกันคือ **แก้อย่างหนึ่งแล้วไปเปลี่ยน
ความหมายของอย่างอื่นที่พึ่งพามันอยู่** — clamp ที่ดูเหมือนไร้ค่า จริง ๆ แล้วกำลังกลบเงื่อนไข
routing เอาไว้ · จุดที่ควรระวังคือเวลาปลด guard ที่ "ไม่เคยทำงาน" ต้องถามว่ามีใครพึ่งพา
การที่มันไม่เคยทำงานอยู่หรือเปล่า

**ข้อสังเกตเรื่องกระบวนการ:** เทสต์ 46 ชุดผ่านหมดทั้งก่อนและหลัง — จับ P2-5 ไม่ได้เลย
สิ่งที่จับได้คือ**การอ่านผลลัพธ์จาก fixture จริง** (`test_engine` พิมพ์ anchor ของทุก ticker
ออกมา) เทสต์ที่ assert ว่า "ไม่ crash" กับเทสต์ที่ให้ **มนุษย์เห็นตัวเลข** มีค่าไม่เท่ากัน
