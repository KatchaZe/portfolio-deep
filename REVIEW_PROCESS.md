# กระบวนการ review ที่ใช้จริง — และทำไมแบบเดิมถึงวนไม่จบ

> สร้าง 2026-08-09 หลัง review 3 รอบใหญ่ + แก้ย่อยอีกหลายรอบ ที่ยัง**เจอของผิดทุกครั้ง**

## หลักฐาน: defect รอบล่าสุด 16 จุด ถูกเจอด้วยวิธีไหน

| วิธี | จำนวน |
|---|---|
| รันข้อมูลจริง แล้วเทียบเลข 2 ตัวที่ควรตรงกัน | **9** |
| ไล่ขอบเขตของ input (ติดลบ · ศูนย์ · หาย · สลับลำดับ · ขาดปี) | **6** |
| เทสต์เดิมจับได้ | **1** |
| **อ่าน code ทีละบรรทัด** | **0** |

และ **เทสต์ผ่าน 100% ก่อนทุกรอบ** — 46 ชุดเขียวสนิทขณะที่ P3-1 (ตัวเลขผิด 2 เท่าบนหน้าจอ) ยังอยู่

**~30% ของ defect อยู่ใน code ที่เขียนตอน review รอบก่อน** — P2-1 คือ code ของ REV-1 · P2-5 เกิดจาก REV-23 · AVGO ROIC 130% คือ code ของ T5 เอง

## ทำไมอ่าน code ไม่เจอ

defect ทุกตัวมีรูปร่างเดียวกัน: **ชื่อของค่า ไม่ตรงกับสิ่งที่อยู่ข้างใน** — คนละนาฬิกา · คนละฐานวัด ·
คนละหน่วย · คนละลำดับ · ไม่ครบแต่ใช้เหมือนครบ · "วัดไม่ได้" ถูกแปลเป็น "ปกติ"

**แต่ละบรรทัดถูกหมดในตัวเอง** ความผิดอยู่ที่ความสัมพันธ์ระหว่างสองบรรทัดที่ห่างกัน 167 บรรทัด
และ `FinancialFacts` มี 48 field ที่มีนาฬิกา = **1,128 คู่ที่จับคู่ผิดได้** — สายตาไม่สเกลกับตัวเลขนี้

---

## เครื่องมือ 4 ชั้นที่สร้างไว้แล้ว

### ชั้น 1 · `tests/test_contracts.py` + `domain/contracts.py` — ปิด defect family ด้วยเครื่อง

ทุก field ประกาศ **นาฬิกา** ของตัวเอง (`TTM` · `FY` · `FY_DATED` · `QUARTER` · `INSTANT` · `SPOT` · `FORWARD`)
แล้ว AST scanner ตรวจว่าไม่มีการ **หาร/ลบ ข้ามนาฬิกา** โดยตามตัวแปรกลางไปด้วย

```python
ann    = f.revenue_annuals or []
rev_1y = ann[1] if len(ann) > 1 else None
a1     = f.revenue / rev_1y - 1        # <- scanner จับได้: revenue(TTM) ÷ revenue_annuals(FY)
```

* **เพิ่ม field ใหม่โดยไม่ประกาศนาฬิกา = เทสต์แดง** — บังคับให้ตอบคำถาม "วัดช่วงไหน" ตอนที่คำตอบยังชัด
* ข้อยกเว้นต้อง**ระบุชื่อฟังก์ชัน** ห้ามยกเว้นทั้งระบบ (ผมพลาดข้อนี้เองรอบแรก — ยกเว้นคู่ P3-1 ทั้งระบบ แล้ว scanner ก็เดินผ่าน P3-1 ไปเฉย ๆ)
* **scanner ถูกทดสอบกับ P3-1 ตัวจริง** — checker ที่ไม่เคยเห็นมันแดง ไม่ใช่ checker

### ชั้น 2 · `tests/test_invariants.py` — ของที่ต้องจริงทุกแถวเสมอ

identity ที่ต้องเป็นจริงข้าม **สอง code path** — ทุกข้อเคยเป็น defect จริงมาก่อน

| | |
|---|---|
| I1 | "actual growth" สองตัวบนการ์ดใบเดียวต้องเท่ากัน (P3-1) |
| I2 | spread = ROIC − WACC บน ROIC ที่การ์ดโชว์จริง (D3) |
| I3 | anchor FV ต้องเป็นค่าที่วิธีใดวิธีหนึ่งคำนวณออกมาจริง |
| I4 | anchor ต้องอยู่ใน range ที่พิมพ์ข้าง ๆ |
| I5 | GARP = E_econ + P |
| I6 | upside% = (anchor − price)/price |
| I7 | watchlist row กับ holdings row ต้องมี field ครบเท่ากัน (`2df2ce2`) |
| I8 | list ไตรมาสต้องอ่านตัวใหม่สุดด้วย**วันที่** ไม่ใช่ตำแหน่ง (B6) |
| I9 | pillar อยู่ใน [0,5] และ composite อยู่ระหว่าง pillar ที่สร้างมัน |

**มี mutation self-test:** ใส่ P3-1 กับ bug ลำดับกลับเข้าไปในหน่วยความจำทุกครั้งที่รัน แล้วยืนยันว่า I1/I8 **แดงจริง**

### ชั้น 3 · `tests/replay_snapshot.py` — จับทุกคะแนนที่ขยับบนบริษัทจริง

```bash
python -m tests.replay_snapshot            # diff กับ baseline · exit 1 ถ้ามีอะไรขยับ
python -m tests.replay_snapshot --update   # ยอมรับค่าใหม่เป็น baseline
python -m tests.replay_snapshot --verbose  # ดูทุกค่า
```

`tests/replay_baseline.json` commit ไปกับ code · **การ update baseline เป็นการกระทำที่ตั้งใจและเห็นใน diff**
คำถามตอน review จึงเปลี่ยนจาก *"มีอะไรเปลี่ยนไหม"* เป็น *"เปลี่ยนตรงนี้ กับบริษัทนี้ ตั้งใจหรือเปล่า"*

> harness นี้เคยอยู่ใน `/tmp` และถูกเขียนใหม่ทุกรอบ — มันคือเครื่องมือที่เจอ 9 ใน 16 จุด

---

## Checklist สำหรับ review รอบถัดไป

**ก่อนแก้**
1. `python -m tests.replay_snapshot` — ต้องสะอาด (ถ้าไม่สะอาด แปลว่ามีของค้างจากรอบก่อน)
2. เปิด `domain/contracts.py` อ่านนาฬิกาของ field ที่จะไปยุ่ง

**ระหว่างแก้**
3. ทุกครั้งที่เขียน `a / b` หรือ `a - b` ถามว่า *สองตัวนี้อยู่นาฬิกาเดียวกันไหม* — ถ้าไม่ ต้องมีเหตุผลใน `ALLOWED` พร้อมชื่อฟังก์ชัน
4. ทุก input ใหม่ ไล่ 6 กรณี: **ติดลบ · ศูนย์ · None · list ว่าง · สลับลำดับ · ปีขาด**
5. ทุก threshold บนอัตราส่วน ถามว่า **อีกฝั่งของเกณฑ์มีเคสซ่อนอยู่ไหม**
   (B5 "3y ≥ 80% ของ 1y" ครอบคลุมทั้งการชะลอตัว — NVO ได้ +0.25 ป้าย "โตต่อเนื่อง" ทั้งที่ growth ตกจาก 20% เหลือ 6%)

**หลังแก้**
6. `python run_tests.py` — 55 ชุด
7. `python -m tests.replay_snapshot` — **อ่านทุกบรรทัดที่ขยับ อธิบายให้ได้ทีละบรรทัด** แล้วค่อย `--update`
8. เทียบของจริง 2–3 ตัวกับตัวเลขที่บริษัทประกาศ (FCF · gross margin · revenue growth)
9. **ตรวจ "การแก้" เป็นรอบแยก** — 30% ของ defect มาจาก code ที่เขียนตอน review

---

## สิ่งที่ยัง**ไม่**ควรทำ

* **อ่าน code ทีละบรรทัด** — 0/16 · codebase นี้คอมเมนต์แน่นมาก ทุกบรรทัดอธิบายตัวเองครบ ความผิดไม่ได้อยู่ในบรรทัด
* **แบ่ง session ตาม module** — defect อยู่ที่**รอยต่อ** (`sec_edgar → normalize → engine → UI`) แบ่งตาม module = ไม่มีใครเป็นเจ้าของรอยต่อ
  ถ้าจะแบ่ง ให้แบ่งตาม **รอยต่อของ data flow**: "อะไรส่งข้ามจากขั้นนี้ไปขั้นนั้น และ หน่วย/นาฬิกา/ลำดับ เปลี่ยนไหม"
* **เพิ่มรอบ review ด้วยวิธีเดิม** — ผลตอบแทนลดลงเรื่อย ๆ และเพิ่มความเสี่ยงที่การแก้จะสร้าง defect ใหม่

## ช่องโหว่ที่ปิดไปแล้ว (2026-08-09 รอบสอง)

| ช่องโหว่เดิม | ปิดยังไง | พิสูจน์ว่าจับได้ |
|---|---|---|
| ประกาศแต่นาฬิกา ไม่มี **หน่วย** | `UNIT` ครบ 79 field + ตรวจว่า **money ทุกตัวถูก FX-convert จริง** โดยดูเฉพาะในบล็อกแปลงค่าเงิน | ถอด `sbc` ออกจาก `_MONEY` → check แดง (= REV-1 ตัวจริง) |
| ตรวจแค่ `/` และ `−` | ขยายไป **การเปรียบเทียบ** (`<` `>` `<=` `>=`) | `f.revenue > ann[1]` → แดง |
| ตามข้ามฟังก์ชันไม่ได้ | `PARAM_CLOCK` ประกาศนาฬิกาที่ **parameter คาดหวัง** แล้วตรวจที่ **call site** | `reverse_dcf(rev_1y=f.revenue)` → แดง "parameter expects FY, got TTM" |
| **basis** (R&D/lease) ไม่ถูกประกาศ | `key_metrics.roic_basis` บันทึกฐานของทุกแถว + I10 ตรวจว่าไม่มีแถวไหนรายงาน trend พร้อมกับบอกว่า skip | 16/21 = `rd_capitalized+leases`, 5/21 = `raw+leases` |

**เมทริกซ์ทดสอบ scanner: 15 เคส ผ่านหมด** (ควรจับ 7 · ควรผ่าน 8) — อยู่ใน `tests/test_contracts.py::test_matrix`

**mutation self-test ตอนนี้ครอบ 6 guard** — ทุกครั้งที่รัน จะใส่ bug เดิมกลับเข้าไปแล้วยืนยันว่าแดง:
P3-1 · ลำดับ Yahoo · FX ตกหล่น · cross-clock comparison · cross-clock call argument · cross-unit division

---

## ชั้น 4 · `pipeline/dataquality.py` — ตรวจ **ข้อมูล** ไม่ใช่ code

สามชั้นแรกตรวจ code ของเรา แต่ defect หลายตัว **ไม่ใช่ bug ของ code เลย** — code ถูก ข้อมูลผิด แล้ว code ก็ประมวลผลด้วยความมั่นใจเต็มที่

```bash
python -m tests.test_dataquality
```

| ตรวจอะไร | ระดับ | เคสจริง |
|---|---|---|
| งบล่าสุดเก่ากว่า 15 / 24 เดือน | warn / **block** | **TSM: SEC หยุดที่ FY2024 = เก่า 20 เดือน** แต่ยังได้ composite 3.95 HOLD/Accumulate |
| ปีงบขาดกลางซีรีส์ | note / **warn** (ถ้าเป็นทุน) | AVGO ขาด FY2022-24 → ROIC เคยอ่านได้ 130% |
| สกุลเงินไม่ใช่ USD แต่ไม่มีร่องรอยการแปลง | **block** | NVO (DKK) ถ้าข้าม FX |
| ประวัติสั้นเกินคำนวณเทรนด์ | note | |

**เจอ TSM ตัวเดียวจาก 21 ชื่อ — ไม่มี false positive** (เกณฑ์แรกที่ลองใช้ "ความยาว series ต่างกัน" ติด 8/21 = noise ที่คนจะเลิกอ่าน จึงทิ้ง)

### ลำดับการทำงานเมื่อเจองบเก่า

```
SEC งบเก่า > 15 เดือน
   └─> ลอง FMP annual statements ก่อน            ← "ได้ตัวเลขใหม่ ดีกว่าเตือนว่าเก่า"
         ├─ ใหม่กว่าจริง → ใช้ scalar จาก FMP
         │                  + **ล้างซีรีส์ย้อนหลังของ SEC ทิ้ง**
         └─ ไม่ใหม่กว่า / ดึงไม่ได้ → flag + หัก confidence
```

**จุดที่อันตรายที่สุดของ fallback คือการล้างซีรีส์** — ถ้าเอา revenue FY2025 จาก FMP มาวางข้าง `revenue_annuals` ของ SEC ที่จบ FY2024 นั่นคือ **P3-1 ที่ถูกสร้างใหม่โดย code ที่เขียนมาแก้ปัญหางบเก่า** · แถบเทรนด์ว่างพร้อมบอกเหตุผล = ถูก · แถบเทรนด์ที่ลากจากสองปีคนละงวด = ผิด

หัก confidence **ครั้งเดียวต่อระดับความรุนแรง** ไม่ใช่ต่อข้อความ — 5 note เรื่องปีขาด คือปัญหาเดียว ไม่ใช่ 5 ปัญหา

---

## ช่องโหว่ที่เหลือ — และที่ **ตั้งใจไม่ทำ**

**ตั้งใจไม่ทำ (วัดแล้วไม่คุ้ม):**

| | วัดได้ | เหตุผล |
|---|---|---|
| alias ข้าม container/comprehension | อ่าน `f.<field>` 250 ครั้ง อยู่ใน comprehension **0 ครั้ง** | ช่องโหว่ทางทฤษฎี ไม่มีของจริงให้ดัก |
| scan JavaScript ใน `index.html` | ทั้งไฟล์มี **2 นิพจน์** ที่ทำเลขกับ field (`x.upside_pct/100`, `x.eva/1e9`) และเป็นการแปลงหน่วยแสดงผล | สร้าง JS AST checker เพื่อ 2 นิพจน์ · I1–I9 คุม contract ระหว่าง backend↔frontend อยู่แล้ว |
| basis นอกเหนือ ROIC | margin/growth/FCF **ไม่มี** raw-vs-adjusted duality | ไม่มีอะไรให้ประกาศ |

**ยังเหลือจริง:**

* `PARAM_CLOCK` ครอบ 7 ฟังก์ชัน — ที่เหลือรับ**ค่าที่คำนวณแล้ว** (`roic_map`, `trend_rows`) ซึ่ง checker ตามไม่ได้อยู่ดี
* data-quality ตรวจ 4 อย่าง — ยังไม่ตรวจ **ค่าที่กระโดดผิดปกติ** (revenue เด้ง 10 เท่าใน 1 ปี = น่าจะแท็กผิด)
* fallback ใช้ FMP อย่างเดียว — ยังไม่มีชั้นที่ 3

**หมายเหตุเรื่องความพอดี:** 55 suite บน app 20k LOC ฟังดูเยอะ แต่ตรวจแล้วมี assertion ซ้ำข้ามไฟล์แค่ **2 จุด** (และตั้งใจซ้ำทั้งคู่) · guard ทุกตัวมี mutation self-test พิสูจน์ว่าแดงได้จริง — **guard ที่ไม่เคยถูกเห็นแดง คือต้นทุนล้วน ๆ** ถ้าวันหนึ่งมี guard ที่พิสูจน์ไม่ได้ว่าจับอะไร ให้ลบทิ้ง
